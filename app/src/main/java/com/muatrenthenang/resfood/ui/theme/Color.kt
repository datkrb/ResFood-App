package com.muatrenthenang.resfood.ui.theme

import androidx.compose.ui.graphics.Color

// Màu chủ đạo (Brand Color)
val PrimaryColor = Color(0xFF339CFF) // Màu xanh ResFood

// Màu nền (Background)
val BgLight = Color(0xFFF5F7F8)      // Nền sáng
val BgDark = Color(0xFF0F1923)       // Nền tối (theo HTML dark mode)

// Màu chữ (Text)
val TextDark = Color(0xFF0F1923)     // Chữ đen đậm
val TextLight = Color(0xFFFFFFFF)    // Chữ trắng
val TextGray = Color(0xFF6B7280)     // Chữ xám phụ

// Tokens thêm cho Cart và UI (không thay đổi theo theme)
val LightRed = Color(0xFFEF4444)     // Dùng cho hành động xóa (light red)
val SuccessGreen = Color(0xFF10B981) // Dùng cho hiển thị giảm giá / thành công
val LightBorder = Color(0xFFE5E7EB)  // Border trong chế độ sáng
val DarkBorder = Color(0xFF2C3942)   // Border khi ở chế độ tối (để phù hợp với BgDark)

// Các màu phụ trợ khác (nếu cần giữ lại màu mặc định của Android)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)